# Source global definitions
if [ -f /etc/bashrc ]; then
	. /etc/bashrc
fi

## This configuration file is read whenever a new interactive shell is started.
## Lines that begin with # are comments.

## To configure your environment for different software packages, use the
## ".swsetuprc" file in your home directory.

## A default prompt is set in a global configuration file.  You can uncomment
## the following line and change it to set a different prompt:

#PS1='\u@\h\\$ '

## If you want to add alias and function definitions, you can put them in
## files named ".bash_aliases" and ".bash_functions" in your home directory.
